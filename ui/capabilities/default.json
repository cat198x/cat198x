{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: it invokes the app's own read-only commands and needs no extra core permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
